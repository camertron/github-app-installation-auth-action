{
  "name": "github-app-installation-auth-action",
  "description": "A GitHub action for retrieving an auth token for a GitHub App installation.",
  "version": "1.0.0",
  "author": "Cameron C. Dutro",
  "license": "MIT",
  "main": "dist/index.js",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.ts src/post.ts --bundle --outdir=dist --platform=node --target=node16.16"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@octokit/auth-app": "^5.0.6",
    "@octokit/request": "^8.1.1",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "@types/node-fetch": "^2.6.4",
    "esbuild": "0.19.2",
    "ts-node": "10.9.1",
    "typescript": "^4.3.5"
  }
}
